<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>hexatorn</groupId>
    <artifactId>my-smaug</artifactId>
    <version>1.0-SNAPSHOT</version>
    <name>my-smaug</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <junit.version>5.12.1</junit.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>25.0.3</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>25.0.3</version>
        </dependency>

        <dependency>
            <groupId>io.github.mkpaz</groupId>
            <artifactId>atlantafx-base</artifactId>
            <version>2.1.0</version>
        </dependency>

        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-javafx</artifactId>
            <version>12.4.0</version>
        </dependency>
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-materialdesign2-pack</artifactId>
            <version>12.4.0</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Asercje testowe: jeden styl assertThat(...) w całym harnessie — TestFX wystawia
             własne API asercji zbudowane na AssertJ, więc nie mieszamy dwóch filozofii. -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.27.7</version>
            <scope>test</scope>
        </dependency>

        <!-- Testy widoków. 4.0.18 (luty 2024) to ostatnie wydanie i zarazem data ostatniego
             commita w repozytorium TestFX — biblioteka jest w trybie utrzymaniowym, a oficjalnie
             deklaruje wsparcie do JavaFX 21. Uruchomienie jej na JFX 25 / Java 23 / module-path
             to główne ryzyko tej zmiany, dowodzone osobną fazą.
             Bez openjfx-monocle: nie istnieje build dla Javy 23/JFX 25, więc testy wymagają
             realnego ekranu. Headless odblokuje się dopiero przy JavaFX 26. -->
        <dependency>
            <groupId>org.testfx</groupId>
            <artifactId>testfx-core</artifactId>
            <version>4.0.18</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testfx</groupId>
            <artifactId>testfx-junit5</artifactId>
            <version>4.0.18</version>
            <scope>test</scope>
        </dependency>

        <!-- Hamcresta nie używamy w kodzie ani razu — potrzebuje go KOMPILATOR. Metody
             FxRobot.lookup i clickOn mają przeciążenia przyjmujące Matcher<Node>, więc żeby
             rozstrzygnąć, który wariant wołamy, javac musi wczytać komplet sygnatur.
             Przychodzi wprawdzie przechodnio z testfx-core, ale wtedy nie trafia na module-path:
             zależności przechodnie lądują tam tylko z własnym module-info, a Hamcrest jest
             modułem automatycznym (org.hamcrest, nazwa z Automatic-Module-Name). Deklaracja
             bezpośrednia wciąga go do rozwinięcia TEST-MODULE-PATH i problem znika.
             Wersja jak u TestFX, świadomie bez podnoszenia. -->
        <dependency>
            <groupId>org.hamcrest</groupId>
            <artifactId>hamcrest</artifactId>
            <version>2.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <release>23</release>
                </configuration>
            </plugin>
            <!-- Runner testów przypięty jawnie: domyślne wiązanie Mavena 3.9 to surefire 2.12.4,
                 sprzed JUnit 5 — bez pinu testy nie wystartowałyby wcale. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.5</version>
                <configuration>
                    <!-- TestFX sięga refleksją do internals JavaFX (ParametersImpl) i w czasie testów
                         działa w module nienazwanym — stąd ALL-UNNAMED. add-opens implikuje
                         add-exports, więc jedna flaga gasi oba ostrzeżenia. -->
                    <argLine>--add-opens javafx.graphics/com.sun.javafx.application=ALL-UNNAMED</argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <executions>
                    <execution>
                        <!-- Default configuration for running with: mvn clean javafx:run -->
                        <id>default-cli</id>
                        <configuration>
                            <mainClass>hexatorn.mysmaug/hexatorn.mysmaug.app.MySmaugApplication</mainClass>
                            <launcher>app</launcher>
                            <jlinkZipName>app</jlinkZipName>
                            <jlinkImageName>app</jlinkImageName>
                            <noManPages>true</noManPages>
                            <stripDebug>true</stripDebug>
                            <noHeaderFiles>true</noHeaderFiles>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>